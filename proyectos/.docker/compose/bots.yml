services:
  asistente_nautico:
    image: python:3.11-slim
    container_name: asistente_nautico
    restart: unless-stopped
    volumes:
      - ../telegram_bot:/app
    working_dir: /app
    command: sh -c "pip install -r requirements.txt && python telegram_bot.py"
